# 写作引用加载项

桌面端运行时会在 `localhost:28886` 启动本机写作桥接服务。它只服务当前机器上的 Word 任务窗格，不对局域网开放。

## Word

在 Word 中进入“插入 → 获取加载项 → 我的加载项 → 管理我的加载项 → 上传我的加载项”，选择同目录的 `word-manifest.xml`。打开任务窗格后输入桌面端“写作引用”页显示的六位配对码。

## WPS

当前版本未开放 WPS 写作适配器。桥接服务会拒绝 WPS 配对和入口请求，后续重新开放前需完成具体 WPS 版本的兼容性验收。
